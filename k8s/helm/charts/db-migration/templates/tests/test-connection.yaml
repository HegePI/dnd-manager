apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "db-migration.fullname" . }}-test-connection"
  labels:
    {{- include "db-migration.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "db-migration.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
